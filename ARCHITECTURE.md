# Asset Explorer V2 架构说明

## 目标与边界

V2 是一次不兼容旧数据的全新架构，优先保证性能、界面响应和故障可恢复性。桌面端目标规模为 100 万文件、10 万文件夹、单文件 50GB；旧 `assethub.db` 不迁移、不删除，V2 使用 `%LOCALAPPDATA%\AssetHub\assethub-v2.db`。

核心约束：前端不持有完整资产表；扫描、监控、数据库写入、缩略图和页面缓存全部有硬上限；普通索引不读取大文件正文；只有一次完整在线扫描成功后才清理本代未见记录。

## 数据流

```text
Windows 文件系统
  ├─ 用户主动添加目录 ─→ 有界流式扫描 ─→ 200 条批次 ─→ SQLite 单写入线程
  └─ notify 文件事件  ─→ 8192 有界队列 ─→ 合并/去抖 ─→ 单路径或子树更新
                                                        │
SQLite WAL ← 文件事实 / 用户状态分离                    │
  ├─ 最多 4 个只读连接 ← 游标 SQL 查询 ← Tauri V2 IPC ← React 查询缓存
  └─ 1 个写入执行器（队列 256）                         └→ 紧凑失效事件

React
  ├─ 工作区摘要：根目录、标签、集合、智能文件夹
  ├─ 资产页缓存：每页最多 300，全局最多 20 页
  ├─ 主视图：仅渲染视口与前后 4 行
  ├─ 双分列：左右独立查询、游标和虚拟滚动
  └─ 缩略图：接近视口才请求，内存 LRU 最多 256
```

## 启动过程

正常启动只做四件事：打开 V2 SQLite、初始化 WAL/索引、注册数据库中已监视的根目录、启动事件处理线程。启动时不校验全部资产、不遍历磁盘、不因窗口聚焦触发对账，因此首屏时间与资产总量基本解耦。

工作区先读取轻量 shell，再发起第一页资产查询。读取错误会显示错误状态，不会被转换成“空数组”，从而避免把数据库或 IPC 故障误判为空工作区。

## 索引与扫描

`IndexCoordinator` 固定 2 个工作线程、等待队列 32，并以根目录作为 single-flight 键；同一个根不会并发执行两次全扫描。扫描器逐项发现文件，按 200 条输出文件夹或资产批次，不保存完整路径列表或完整扫描结果。

每次全扫描先取得递增 generation。写入的文件夹和资产都记录 `root_id` 与 `last_seen_generation`。只有收到扫描器 `Finished` 且数据库成功提交时，才软删除同根下未在本代出现的资产并清理失效文件夹。取消、权限错误、磁盘离线、进程退出或任意批次失败都不会执行清理。

普通扫描只读取目录项、文件大小、mtime 和必要的图片头；不会计算全文件 SHA-256，也不会把几十 GB 文件读入内存。

## 实时监控

notify 回调只尝试写入容量 8192 的同步队列，不执行数据库、图片或递归扫描工作。事件处理器按规范化路径合并 Create/Modify/Remove/Replace，目录事件覆盖其子路径冗余事件。

文件变化只 stat 并更新一个路径；目录创建只调度该子树的有界扫描；目录删除执行路径前缀事务。不会按单个事件启动线程，也不存在 5 秒轮询、窗口回焦全扫或自动深度对账。队列溢出会计入诊断指标，恢复操作必须由用户显式启动。

完整恢复通过 `start_integrity_job_v2` 显式启动。它仅接受已经登记的监控根目录，复用同一有界扫描协调器和代次提交规则，并可通过 `cancel_job_v2` 取消；正常启动不会调用它。

## 数据库并发

SQLite 使用 WAL、`synchronous=NORMAL`、外键和 5 秒 busy timeout。查询通过最多 4 个只读连接并发执行；所有修改通过单一写入执行器串行提交，等待任务最多 256。事务中只做 SQL，不夹带磁盘遍历或 UI 事件。

文件系统事实与用户数据分表：扫描只能修改路径、名称、类型、大小、mtime、尺寸、代次和状态；评分、收藏、颜色、自定义名称、备注、标签、集合只能由显式用户操作修改。文件暂时消失时资产行软删除，用户状态仍保留，重新发现同一 ID 后自动恢复。

## 查询与界面

资产与文件夹通过游标分页，单页硬上限 300。排序始终带 ID 作为稳定次级键；名称、mtime、大小、类型、文件夹、路径和用户状态均有对应索引。详情接口只接受明确 ID，最多 1000 个。

React 的 `useAssetQuery` 对等价查询生成稳定键，拒绝旧请求覆盖新查询，并用 20 页 LRU 控制内存。文件事件只触发查询失效，不把完整资产对象不断追加到全局状态。

主网格与列表采用固定行高窗口化，DOM 数量只与窗口尺寸相关。靠近末端时再加载下一页。双分列的两侧各自调用分页查询，互不共享游标，也不遍历完整文件夹树。缩略图通过 IntersectionObserver 在视口前 400px 预取。

## 缩略图与预览

缩略图后端最多允许 512 个请求入队、2 个并发解码；在解码前拒绝声明尺寸超过 1 亿像素的图片。磁盘缓存键包含规范化源路径、文件大小、mtime 和目标尺寸，因此源文件变化不会复用旧缩略图。

前端缩略图内存缓存最多 256 项，并在淘汰时释放对象 URL。普通文件预览使用 Tauri 资源 URL，由协议层流式读取，不存在通用“整个文件转 Base64”命令，因此 50GB 文件不会被复制进 Rust/IPC/JavaScript 内存。文本/文件头检查可使用 `read_asset_range_v2`，该命令先规范化并校验路径位于已监控根目录内，再按 offset 读取，单次硬上限 4MB。

## Web 模式

Web Provider 与桌面端共享 WorkspaceShell、AssetPage、FolderPage、AssetDetail 契约。Express `/api/v2` 接口同样将页面限制为 300；PostgreSQL 对用户+路径、文件夹排序、mtime、大小、收藏/评分建立索引，多对多关联使用复合主键。评分和收藏是真实持久化操作，不再是空实现。

## 诊断与容量

`get_diagnostics_v2` 返回聚合指标：资产数、活动索引任务、数据库待写任务、缩略图请求、查询次数、平均查询微秒数、错误数和监控丢事件数。指标不保存文件路径或逐文件成功日志。

`scripts/generate-scale-fixture.ps1` 可生成指定数量的目录/文件和可选稀疏大文件。它拒绝盘符根目录、用户目录和未经确认的非空目标。

## 故障语义

- 数据库初始化失败：显示原生错误并退出，不回退内存数据库。
- 查询失败：界面显示错误，不伪装为空数据。
- 扫描失败/取消：已有数据保持可见，不清理未见记录。
- 根目录离线：缓存保持，只标识不可更新。
- 监控队列溢出：计数并等待显式恢复，不偷偷启动全盘扫描。
- 缩略图过载：拒绝超额请求，主数据查询仍可响应。

## 关键模块

| 模块 | 职责 |
| --- | --- |
| `database.rs` | V2 schema、读连接池、写入执行器、事实与用户状态持久化 |
| `asset_query.rs` | 参数化过滤、稳定游标、资产/文件夹/详情分页 |
| `indexer.rs` | 不收集全量快照的批次扫描 |
| `index_jobs.rs` | 有界、可取消、按根 single-flight 的任务协调 |
| `event_coalescer.rs` | 纯事件状态机、路径去重与目录支配 |
| `watcher.rs` | notify 收集、目标路径更新和子树调度 |
| `thumbnail_cache.rs` / `thumbnail_jobs.rs` | 版本缓存、安全限制和并发准入 |
| `preview_stream.rs` | 监控根授权与 4MB 上限随机区间读取 |
| `metrics.rs` | 无逐文件载荷的聚合诊断 |
| `useAssetQuery.ts` | 有界页面缓存、失效和旧响应保护 |
| `VirtualAssetGrid/List.tsx` | 视口级 DOM 渲染 |
